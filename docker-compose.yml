services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: eduwave
    ports:
      - "5432:5432"
    volumes:
      - eduwave_db:/var/lib/postgresql/data

volumes:
  eduwave_db:
